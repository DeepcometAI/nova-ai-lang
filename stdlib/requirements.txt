# NOVA Standard Library Dependencies
# Full installation with all features (including astropy for FITS)
# 
# Install using one of:
#   pip install -r stdlib/requirements.txt --only-binary :all:
#   pip install -r stdlib/requirements-minimal.txt  (without astropy)
#
# Core dependencies (always required)
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0
matplotlib>=3.7.0
polars>=0.19.0

# Optional: For FITS file support (astronomy)
# Note: May require pre-built wheels or compilation
astropy>=5.3.0; platform_machine != 'aarch64'
pyerfa>=2.0.0; platform_machine != 'aarch64'
